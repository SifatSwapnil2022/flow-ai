import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  // Remove 'engine' and manual 'datasource' 
  // Prisma will pull from your .env automatically now
});